spring:
  datasource:
    url: jdbc:postgresql://database-1.c5e2usce6r1z.ap-northeast-2.rds.amazonaws.com:5432/pmDatabase
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver


  jpa:
      hibernate:
          ddl-auto: create
      show-sql: true
      properties:
          hibernate:
          format_sql: true
          use_sql_comments: true